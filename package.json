{
  "name": "proagente-gmail-sync",
  "version": "1.0.0",
  "description": "Gmail sync service for Pro Agente CRM",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "googleapis": "^128.0.0",
    "@vercel/kv": "^1.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
